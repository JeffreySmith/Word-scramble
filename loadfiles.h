#ifndef LOADFILES_H
#define LOADFILES_H
#include <iostream>
#include <vector>
using namespace std;
class loadFiles
{
public:
    loadFiles();
    vector<string> parsedText;
private:
    std::string line;

};

#endif // LOADFILES_H
